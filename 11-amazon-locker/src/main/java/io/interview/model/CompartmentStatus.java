package io.interview.model;

/**
 * This will be required for tracking the compartment status.
 * If we have to do some maintenance for a compartment, we can use this enum to track it.
 */
public enum CompartmentStatus {
    AVAILABLE,
    OCCUPIED,
    OUT_OF_SERVICE
}
